{
  "version": "0.2.7",
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "build": "polkadot-dev-build-babel",
    "check": "eslint packages && flow check",
    "test": "jest packages"
  },
  "devDependencies": {
    "@polkadot/dev": "^0.12.4",
    "lerna": "^2.5.1"
  }
}
