{
  "version": "0.2.4",
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "build": "polkadot-dev-build-babel",
    "check": "eslint packages && flow check",
    "test": "jest --coverage packages"
  },
  "devDependencies": {
    "@polkadot/dev": "^0.10.1",
    "lerna": "^2.5.1"
  }
}
